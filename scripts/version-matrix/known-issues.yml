# Known issues and excluded versions for PoS version compatibility testing.
#
# Versions listed here are excluded from the pairwise compatibility matrix
# because of known bugs, regressions, or incompatibilities.
#
# Format:
#   excluded_versions:
#     <component>:
#       "<version>": "<reason>"
#
# Example:
#   excluded_versions:
#     bor:
#       "2.5.0": "Consensus bug in sprint boundary handling (fixed in 2.5.1)"
#     heimdall-v2:
#       "0.5.0-beta1": "Checkpoint signing regression"

excluded_versions: {}
