########################################
# BATS specific variables
########################################

# Path to shared BATS helper libraries
BATS_LIB_PATH="$PWD/core/helpers/lib"

# Root of the project for relative path resolutions
PROJECT_ROOT="$PWD"

########################################
# Network related variables
########################################

# L1 JSON-RPC endpoint (e.g. local Geth or Hardhat)
L1_RPC_URL=http://localhost:8545

# L2 node RPC endpoint (e.g. local OP node or zkEVM node)
L2_RPC_URL=http://localhost:9545

# L2 sequencer RPC endpoint (used when different from L2_RPC_URL)
L2_SEQUENCER_RPC_URL=http://localhost:9645

# REST endpoint for aggkit bridge service
AGGKIT_BRIDGE_URL=http://localhost:8745

# JSON-RPC endpoint for aggkit API service
AGGKIT_RPC_URL=http://localhost:8845

# L2 zkEVM bridge service URL (optional for the aggkit tests, but mandatory for the cdk ones)
ZKEVM_BRIDGE_URL=http://localhost:8945

# Deployed L1 bridge contract address
L1_BRIDGE_ADDRESS=0x1111111111111111111111111111111111111111

# Deployed L2 bridge contract address
L2_BRIDGE_ADDRESS=0x2222222222222222222222222222222222222222

# Address of the token used on L1 for bridging (e.g. POL)
POL_TOKEN_ADDRESS=0x3333333333333333333333333333333333333333

# Address of GlobalExitRoot on L2
L2_GER_ADDRESS=0x4444444444444444444444444444444444444444

# Address of the gas token used on L2 (e.g., native ETH or ERC20)
GAS_TOKEN_ADDRESS=0x5555555555555555555555555555555555555555

# Private key used by the sender account for signing transactions
SENDER_PRIVATE_KEY=0xabcdefabcdefabcdefabcdefabcdefabcdefabcdefabcdefabcdefabcdefabcdef

# If true, disables funding accounts before tests
DISABLE_FUNDING=true


########################################
# Multiple rollups (multi-chain setup)
########################################

# RPC endpoints for each L2 rollup instance
L2_RPC_URL_1=http://localhost:9646
L2_RPC_URL_2=http://localhost:9647
L2_RPC_URL_3=http://localhost:9648

# Aggkit REST bridge service URLs for each rollup
AGGKIT_BRIDGE_1_URL=http://localhost:9746
AGGKIT_BRIDGE_2_URL=http://localhost:9747
AGGKIT_BRIDGE_3_URL=http://localhost:9748

# Gas token addresses for each rollup
GAS_TOKEN_ADDRESS_ROLLUP_1=0xaaaaaaaabbbbbbbbccccccccddddddddeeeeeeee
GAS_TOKEN_ADDRESS_ROLLUP_2=0xbbbbbbbbccccccccddddddddeeeeeeeeffffffff
GAS_TOKEN_ADDRESS_ROLLUP_3=0xccccccccddddddddeeeeeeeeffffffff00000000
